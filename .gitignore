# Rust (workspace target at repo root)
/target/

# Synapse local stores (per-project event + snapshot data)
.synapse/

# npm package — installer-downloaded binary + deps (not source)
npm/node_modules/
npm/bin/synapse-bin
npm/bin/synapse.exe

# Tooling / local config
.cortex/
.claude/

# OS / editor
.DS_Store
Thumbs.db
